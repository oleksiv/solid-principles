/**
 * Приклад порушення принципу інверсії залежностей (DIP) в системі логування
 *
 * Проблеми цього коду:
 * - UserService жорстко прив'язаний до console.log
 * - Не можемо змінити спосіб логування без зміни UserService
 * - На продакшені можемо хотіти логувати в файл або відправляти на віддалений сервер
 * - Складно тестувати — не можемо контролювати логування
 * - Бізнес-логіка змішана з технічними деталями логування
 */

export class UserService {
    createUser(name: string, email: string): void {
        // Бізнес-логіка створення користувача
        console.log(`Створюю користувача ${name}`);

        // 🚨 Проблема: жорстко прив'язані до console.log
        console.log(`Користувач ${name} створений успішно`);
    }

    deleteUser(id: string): void {
        // Бізнес-логіка видалення
        console.log(`Видаляю користувача з ID: ${id}`);

        // 🚨 Проблема: знову жорстка залежність від console.log
        console.log(`Користувач ${id} видалений`);
    }
}
