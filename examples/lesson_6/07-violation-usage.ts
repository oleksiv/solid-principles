/**
 * Приклад використання класів, які порушують принцип інверсії залежностей
 *
 * Цей файл демонструє проблеми, які виникають при жорстких залежностях:
 * - Неможливо змінити реалізацію без зміни коду
 * - Складно тестувати
 * - Код негнучкий та важко розширюваний
 */

import { PayPalPayment, OrderService } from './01-payment-violation';
import { UserService } from './04-logging-violation';

console.log('=== Демонстрація проблем порушення DIP ===');

// 1. Проблема з платіжною системою
console.log('\n1. Платіжна система (жорстка залежність від PayPal):');
const orderService = new OrderService();
orderService.processOrder(100);

// 🚨 Проблема: Не можемо використати Stripe або банківську карту
// 🚨 Проблема: Не можемо протестувати з мок-об'єктом
// 🚨 Проблема: Щоб додати новий спосіб оплати, треба змінювати OrderService

console.log('\n❌ Спроба додати Stripe потребує зміни OrderService!');
console.log('❌ Неможливо протестувати без реальних PayPal викликів!');

// 2. Проблема з системою логування
console.log('\n2. Система логування (жорстка залежність від console.log):');
const userService = new UserService();
userService.createUser('Іван', 'ivan@example.com');
userService.deleteUser('user123');

// 🚨 Проблема: Завжди логує в консоль
// 🚨 Проблема: На продакшені хочемо логувати в файл або на сервер
// 🚨 Проблема: Не можемо контролювати логування в тестах

console.log('\n❌ Завжди логує тільки в консоль!');
console.log('❌ Неможливо змінити спосіб логування без зміни UserService!');
console.log('❌ Складно тестувати - не можемо перехопити логи!');

console.log('\n=== Висновок ===');
console.log('Жорсткі залежності роблять код:');
console.log('- Негнучким (важко змінювати)');
console.log('- Важким для тестування');
console.log('- Складним для розширення');
console.log('- Порушують принцип відкритості/закритості');
console.log('\nРішення: використовувати абстракції та Dependency Injection!');
